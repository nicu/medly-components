import { WithStyle } from '@medly-components/utils';
import { FC } from 'react';
import { HeaderProps, HeaderStaticProps } from './types';
declare const Header: FC<HeaderProps> & HeaderStaticProps & WithStyle;
export default Header;
