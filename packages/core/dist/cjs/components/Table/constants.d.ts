import { KeyBindings, TableColumnConfig } from './types';
export declare const rowActionsColumnConfig: TableColumnConfig;
export declare const loadingBodyData: {
    id: string;
}[];
export declare const defaultKeyBindings: KeyBindings;
