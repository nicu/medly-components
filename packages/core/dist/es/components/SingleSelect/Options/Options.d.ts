import { WithStyle } from '@medly-components/utils';
import type { FC } from 'react';
import { OptionsProps } from './types';
declare const Options: FC<OptionsProps> & WithStyle;
export default Options;
